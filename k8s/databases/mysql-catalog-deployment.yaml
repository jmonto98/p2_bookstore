apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-catalog
  namespace: bookstore
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-catalog
  template:
    metadata:
      labels:
        app: db-catalog
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: "password"
            - name: MYSQL_USER
              value: "user"
            - name: MYSQL_PASSWORD
              value: "password"
            - name: MYSQL_DATABASE
              value: "bookstore_catalog"
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: mysql-catalog-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-catalog-storage
          persistentVolumeClaim:
            claimName: mysql-catalog-pvc
