apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: bookstore
spec:
  selector:
    app: rabbitmq
  ports:
    - name: amqp
      port: 5672
      targetPort: 5672
      nodePort: 15672
  type: NodePort